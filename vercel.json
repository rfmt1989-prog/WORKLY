{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd frontend && npm ci --no-audit --no-fund && npx expo export --platform web --output-dir web-build",
  "outputDirectory": "frontend/web-build"
}
