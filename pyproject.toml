[project]
name = "workly-demo-api"
version = "1.0.0"
description = "WORKLY FastAPI demonstration backend"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.117.1,<1.0",
  "email-validator>=2.2.0,<3.0",
]

[tool.vercel]
entrypoint = "backend.app.main:app"

